# 班务管理系统

一个面向班主任的班级成绩记录与学情管理工具，帮助教师高效完成学生成绩录入、班级总览分析、学习报告导出与期末评语撰写等工作。

## 体验地址

https://limuyi1.github.io/class-management-system

## 功能特性

### V1.0 成绩录入 ✅

- **快速录入成绩** - 支持姓名/拼音模糊匹配，快速定位学生
- **回车键分流** - 姓名框回车仅定位学生，分数框回车保存并回到姓名输入框
- **AI 识图导入** - 中栏提供 AI 识图导入（批量）入口，识别后写入当前录入科目
- **Excel 导出** - 工具栏统一导出入口（当前成绩 / 全部成绩）
- **自定义导入** - 支持导入自定义学生名单
- **数据持久化** - 浏览器本地存储，刷新页面数据不丢失

### V2.0 期末评语 ✅

- **评语录入** - 支持手动输入评语或从 Excel 导入
- **个性化配置** - 支持设置字体大小、落款签名、页面纸张类型（A3/A4/B3/B4）
- **PDF 导出** - 一键导出评语为 PDF 格式
- **综合评比率** - 实时统计综合评分
- **图片导出** - 支持导出低分学生名单为图片，方便发送到班级群

### V3.0 班级总览 + 学习报告 ✅

- **全新桌面化布局**
  - 左侧菜单导航，统一切换总览、成绩、评语、设置等页面
  - 4 套主题颜色（清新绿、阳光橙、优雅紫、柔和蓝粉）
  - 页面头部、卡片、图表和弹层样式整体重构，信息密度更适合教师桌面端场景

- **班级总览页**
  - 班级 KPI、单元完成度、重点学生、关注分组等核心信息集中展示
  - 学生趋势分析支持单人聚焦与多人对比，快速查看阶段成绩变化
  - 待写评语数量、AI 配置状态、学情诊断入口统一放到总览页
  - 支持基于当前班级数据生成 AI 学情分析

- **学习报告导出**
  - 从成绩页或总览页直接打开学生学习报告
  - 自动汇总阶段成绩、班级名次、均分差、趋势变化、标签画像与优势关注点
  - 支持 AI 生成报告正文，也支持在模板基础上手动修改
  - 按设计稿样式导出高清 PNG，适合发送家长群或留档

- **成绩录入增强**
  - 多列成绩管理（期中、期末、月考、单元测等）并支持动态切换当前录入科目
  - 三栏布局：学生列表 / 录入面板 / 统计分析
  - 学生列表按成绩区间着色，低分预警和分布统计优先展示
  - 支持查看单个学生详情，并从详情继续导出学习报告

- **期末评语增强**
  - 支持手动撰写、Excel 导入、快捷评语、AI 生成等多种模式
  - 支持字体大小、落款签名、纸张类型（A3/A4/B3/B4）等打印参数配置
  - 支持评语 PDF 导出，适合批量打印和归档

- **设置中心**
  - **学生信息管理** - 增删改学生、批量打标签、维护名单
  - **标签维护** - 灵活管理标签分类与标签项，支持 AI 生成标签
  - **单元配置** - 动态添加/删除成绩列（支持期末、期中、月考等）
  - **AI 配置** - 支持 Gemini 与 OpenAI 兼容接口，支持测试连接与拉取模型
  - **系统备份** - 完整数据导出/导入，支持数据差异比对与恢复

## 技术栈

| 类别     | 技术                                  |
| -------- | ------------------------------------- |
| 框架     | Vue 3.3 (Composition API)             |
| 构建工具 | Vite 5                                |
| 语言     | TypeScript 5.3                        |
| UI 组件  | Element Plus, VxeTable                |
| 状态管理 | Pinia                                 |
| 数据库   | Dexie (IndexedDB 封装)                |
| 样式     | Tailwind CSS 4, SCSS                  |
| 工具库   | xlsx, pdf-lib, dom-to-image, pinyin-pro |
| 图标     | FontAwesome                           |
| AI 集成  | Gemini, OpenAI 兼容 API              |

## 项目结构

```
src/
├── ai/                   # AI 服务与 Provider 封装
├── assets/               # 静态资源
├── components/           # 公共组件
│   ├── PageHeader.vue    # 页面标题组件
│   ├── OverlengthTextTooltip.vue
│   ├── ThemeSelector.vue
│   └── student-report/   # 学习报告相关组件
├── config/               # 配置文件
│   ├── menu.ts           # 菜单配置
│   ├── theme.ts          # 主题配置
│   └── score.ts          # 分数区间配置
├── hooks/                # 组合式函数
│   ├── useProgress.ts    # 进度计算
│   ├── useScoreStatistics.ts
│   └── useScoreDistributionActions.ts
├── router/               # 路由配置
├── stores/               # Pinia 状态管理
│   ├── data-source.ts    # 学生数据与统计
│   ├── setting.ts        # 表头与标签配置
│   ├── configuration.ts  # 应用配置
│   ├── ai-config.ts      # AI 配置
│   ├── wrong-book.ts     # 错题本数据
│   └── theme.ts          # 主题状态
├── types/                # TypeScript 类型定义
├── utils/                # 工具函数
│   ├── xlsxUntil.ts      # Excel 导入导出
│   ├── pdfUntil.ts       # PDF 导出
│   ├── evaluationTextPdfUntil.ts
│   ├── studentReportUntil.ts
│   └── pageSizeInPixelUntil.ts
└── views/                # 页面组件
    ├── overview/         # 班级总览页
    ├── score/            # 成绩录入页面
    ├── evaluation/       # 期末评语页面
    ├── wrong-book/       # 错题本页面
    ├── setting/          # 设置页面
    └── main/             # 应用主框架
```

## 快速开始

### 安装依赖

```bash
pnpm install
```

### 开发模式

```bash
pnpm dev
```

### 构建生产版本

```bash
pnpm build
```

构建产物默认输出到 `dist/` 目录。

### 类型检查

```bash
pnpm type-check
```

### 代码规范检查

```bash
pnpm lint
```

## 数据说明

### 学生数据存储

- 学生数据通过 Dexie 存储在浏览器 IndexedDB 中
- 数据结构示例：
  ```json
  {
    "xing4_ming2": "张三",
    "yu3_wen2": 85,
    "shu4_xue2": 92,
    "comment": "该生学习认真...",
    "tags": { "优点": ["认真", "积极"] }
  }
  ```

### 表头配置

- 首列固定为 `xing4_ming2`（姓名），不可删除
- 其他列可通过设置页面动态添加、删除、排序

### 最近录入记录

- 当前实现为按科目分组持久化（写入配置存储，刷新后保留）
- 每个科目最多保留 5 条，超出后按时间保留最新记录

## 开发指南

### 添加新的分数列

1. 进入「设置」页面
2. 在「单元配置」中添加新列（输入列名，自动转换为拼音属性名）
3. 在「成绩录入」页面选择对应的列进行录入

### 导出成绩

1. 进入「成绩录入」页面
2. 点击顶部工具栏「导出总表」
3. 选择导出格式（下载当前成绩 / 下载所有成绩）

### 导出学习报告

1. 在「成绩录入」页点击学生详情，或在「班级总览」页打开学生趋势分析
2. 选择目标学生后点击「导出学习报告」
3. 勾选需要纳入报告的成绩项
4. 使用 AI 或模板生成正文，并按需手动修改
5. 选择图片质量与分辨率后导出 PNG

### 成绩录入交互说明

1. 姓名输入框回车：仅定位学生并聚焦到分数输入框
2. 分数输入框回车：保存当前学生分数，清空并回到姓名输入框
3. 清空姓名输入框：同步清除左侧学生列表选中态
4. AI 识图导入：识别结果写入当前录入科目

### 批量打印评语

1. 进入「期末评语」页面
2. 配置页面类型、落款等信息
3. 点击「导出 PDF」按钮

## 后续规划

- [ ] 云端数据存储

## 许可证

MIT
